[project]
name = "task-cli"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
task-cli = "task_cli.main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"